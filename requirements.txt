# ============================================================
# requirements.txt — Project Dependencies
# ============================================================
#
# These are all the external libraries our project needs.
#
# Install everything at once with:
#   pip install -r requirements.txt
#
# Each line is: library_name==version_number
# The == pins the exact version to avoid "it worked on my machine" problems
# ============================================================


# ── Core Libraries ───────────────────────────────────────────

# httpx: Modern HTTP client with async support
# Used for: sending requests to the Gemini API asynchronously
# Why not 'requests'? requests doesn't support async/await
httpx>=0.27.0

# python-dotenv: Reads .env files and sets environment variables
# Used for: loading GEMINI_API_KEY and other settings from .env
python-dotenv>=1.0.0

# pydantic: Data validation using Python type hints
# Used for: validating configuration, request/response data
# v2 is much faster than v1 (uses Rust internally)
pydantic>=2.0.0


# ── AI / LLM Libraries ───────────────────────────────────────

# google-generativeai: Official Google Gemini SDK
# Used for: (reference/fallback — we use the REST API directly via httpx)
# Install it anyway for its utilities
google-generativeai>=0.7.0


# ── Retry Logic ──────────────────────────────────────────────

# tenacity: Retry library with exponential backoff
# Used for: automatically retrying failed API calls
tenacity>=8.2.0


# ── CLI/UI Libraries ─────────────────────────────────────────

# rich: Beautiful terminal output with colors, tables, panels
# Used for: the colorful CLI interface (panels, colors, markdown rendering)
rich>=13.0.0


# ── Type Checking (Development) ──────────────────────────────
# These are optional but good practice for larger projects
# mypy>=1.0.0        # Static type checker
# pyright>=1.1.0     # Microsoft's type checker
